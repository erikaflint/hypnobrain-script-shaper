// Vitest setup file
// This runs before all tests

import { beforeAll, afterAll } from 'vitest';

// Mock environment variables if needed
beforeAll(() => {
  // Setup code here
});

afterAll(() => {
  // Cleanup code here
});
